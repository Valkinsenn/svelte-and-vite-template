/* For SPA (single-page application) routes. */

// Imported Pages
import Home from "../pages/Home.svelte"

// Exported Routes
export const routes = {
    "/": Home
    // Add more pages from here on down
}